# Wellfarm workspace

This workspace contains the active Wellfarm React application, local API, shared generated clients, and development launcher.

Use the repository-level `npm run dev` command. Product behavior and boundaries are defined in the root documentation.
