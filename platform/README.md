# Wellfarm Platform

The runnable Wellfarm application is maintained under `platform/replit`. It originated from a Replit workspace and now serves as the active web and API platform for the standalone portfolio project.

Run it from the repository root with `npm run dev`.
