# Officials' Dashboard Source

Place dashboard source here when the frontend and mapping libraries are selected.

Suggested feature boundaries:

```text
features/
  map/
  district-overview/
  cases/
  referrals/
  recommendations/
  reports/
lib/
  api/
  maps/
  charts/
  auth/
```

Maps must include a legend and textual severity values for users who cannot distinguish the color gradient.

