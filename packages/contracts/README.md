# Shared Contracts

This package defines scan payloads, weather context, model metadata, severity labels, and data provenance shared by the web application and API.

Supported provenance values are `field`, `public_dataset`, `sample`, and `model`. No contract represents laboratory routing, official assignment, or third-party case submission.
